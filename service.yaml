apiVersion: v1
kind: Service
metadata:
  name: dockerminikubelearn-service
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: dockerminikubelearn
  type: LoadBalancer
status:
  loadBalancer: {}
